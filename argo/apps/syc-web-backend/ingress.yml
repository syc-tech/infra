apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: syc-web-ingress
  namespace: syc
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    # nginx.ingress.kubernetes.io/rewrite-target: /
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: api.syctech.io
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: syc-web-service
            port:
              number: 80
  tls:
  - hosts:
    - api.syctech.io
    secretName: syc-web-tls
